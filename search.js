/*$(document).ready(function () {
    // Array of image classes
    var imageClasses = [
        "advertising-1",
        "advertising-2",
        "advertising-3",
        "advertising-4",
        "advertising-5"
    ];

    // Select a random image class from the array
    var randomClass = imageClasses[Math.floor(Math.random() * imageClasses.length)];

    // Get the source of the selected image class
    var randomImageSrc = $("." + randomClass).attr("src");

    // Set the source of the random image
    $("#random-image").attr("src", randomImageSrc);
});*/